# ==============================================
# ChiliScope Frontend - Production Configuration
# ==============================================
# Vite automatically loads this file during `npm run build`
# Values here override .env for production builds

# Use relative path — nginx proxies /api to the backend container
VITE_API_URL=/api/v1
